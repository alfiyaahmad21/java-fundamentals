package OOPs.abstraction;
//An interface can only contain abstract methods

//An interface cannot be instantiated, i.e. we can't create object of any interface

public interface Human {
    void cook();

    void code();

    void play();
}